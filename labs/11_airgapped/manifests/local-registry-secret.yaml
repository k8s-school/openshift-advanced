# Reference: pull secret for an authenticated local registry.
# Uncomment and populate when the registry requires credentials.
#
# apiVersion: v1
# kind: Secret
# metadata:
#   name: local-registry-pull-secret
#   namespace: airgapped-<user>
# type: kubernetes.io/dockerconfigjson
# stringData:
#   .dockerconfigjson: |
#     {
#       "auths": {
#         "localhost:5000": {
#           "username": "user",
#           "password": "password"
#         }
#       }
#     }
